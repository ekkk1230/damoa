version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: damoa-db
    restart: always
    environment:
      MYSQL_DATABASE: damoa
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3307:3306"
    volumes:
      - ./db_data:/var/lib/mysql